name = "oshwhub-data-updater"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[triggers]
crons = ["0 0 * * *"]  # 每天 UTC 0 点运行

[vars]
# 在 Cloudflare 控制台添加以下 secrets:
# REBUILD_HOOK_URL - 你的部署触发钩子 URL
